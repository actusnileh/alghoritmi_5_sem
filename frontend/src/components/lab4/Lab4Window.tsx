import { Container, Title } from "@mantine/core";
import { FC } from "react";

export const Lab4Window: FC = ({}) => {
    return (
        <Container my="xl">
            <Title order={2} mb="xl">
                Лаб. работа №4
            </Title>
        </Container>
    );
};
